exports.initRemov = function(arr1, arr2) {
  const contador = 0;

  for (i = 0; i < arr2.length; i++) {
    if (arr2[i] == "/" && arr2[i + 1] == "/") {
      contador++;
    }

    if (contador == 1) {
    }
  }
};
